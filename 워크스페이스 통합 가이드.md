# Team2 ROS2 프로젝트 작업 구조 정리

> **목적:** 지금까지 각자 다른 실습 워크스페이스와 경로에서 작업하던
> 코드를, 나중에 한꺼번에 합치다가 문제가 생기지 않도록 **팀 프로젝트용
> 구조를 지금부터 통일**합니다.

------------------------------------------------------------------------

## 1. 왜 지금 구조를 바꿔야 하나요?

현재는 팀원마다 개인 브랜치에서 작업하고 있지만, 단순히 브랜치만 다른
것이 아니라 **코드가 놓여 있는 위치와 워크스페이스 구조도 서로
다릅니다.**

예를 들면 다음과 같은 상태일 수 있습니다.

``` text
팀원 A
~/gong_ws/src/tf2_basic/...

팀원 B
~/gong_ws/src/project2/...

팀원 C
~/project2/...

팀원 D
~/team2/ros_ws/src/tictactoe_vision/...
```

각자의 컴퓨터에서는 실행될 수 있지만, 마지막에 브랜치를 합치면 다음
문제가 한꺼번에 발생할 수 있습니다.

-   같은 역할의 파일이 서로 다른 폴더에 있음
-   실습용 패키지와 프로젝트 코드가 섞임
-   launch/config 파일에서 경로가 맞지 않음
-   `/home/사용자명/...` 같은 절대경로가 다른 컴퓨터에서 동작하지 않음
-   `package.xml`, `setup.py` 등의 수정 내용이 충돌함
-   한 사람에게만 존재하는 실습 파일에 프로젝트 코드가 의존함
-   통합 후 `colcon build`는 되지만 실행 시 패키지나 파일을 찾지 못할 수
    있음

따라서 **모든 기능을 완성한 뒤 마지막에 합치는 것보다, 지금 프로젝트의
기준 구조부터 통일하는 것이 안전합니다.**

------------------------------------------------------------------------

## 2. 핵심: 각자 새로운 이름의 워크스페이스를 만드는 것이 아닙니다

가장 중요한 부분입니다.

팀원마다 아래처럼 서로 다른 프로젝트 워크스페이스를 새로 만드는 것이
아닙니다.

``` text
❌ 잘못된 방향

팀원 A → gong_ws
팀원 B → project_ws
팀원 C → my_ws
팀원 D → ros_ws
```

대신 **GitHub의 `team2` 저장소 안에 있는 하나의 동일한 워크스페이스
구조를 모든 팀원이 사용합니다.**

기준 구조:

``` text
team2/
├── .gitignore
├── README.md
└── ros_ws/
    └── src/
        ├── tictactoe_vision/
        ├── tictactoe_manipulator/
        ├── tictactoe_game/
        └── ...
```

각 팀원의 컴퓨터에는 같은 저장소가 clone되므로 결과적으로 다음과 같이
됩니다.

``` text
팀원 A PC
~/team2/ros_ws/

팀원 B PC
~/team2/ros_ws/

팀원 C PC
~/team2/ros_ws/
```

즉 **각자 로컬 복사본은 있지만 Git 내부의 폴더 구조는 모두 동일**합니다.

------------------------------------------------------------------------

## 3. 그러면 기존 작업 파일(gong_ws 등)는 어떻게 하나요?

**삭제하지 않습니다.**

기존 실습 워크스페이스는 그대로 남겨두고 참고용으로 사용하면 됩니다.

예:

``` text
~/gong_ws/                  ← 기존 ROS2 실습용
│   └── src/
│       ├── tf2_basic/
│       ├── ros_basic/
│       └── camera_opencv/
│
└── (별개)

~/team2/                    ← 팀 프로젝트용 Git 저장소
    └── ros_ws/
        └── src/
            ├── tictactoe_vision/
            └── ...
```

앞으로 **팀 프로젝트 코드는 `team2/ros_ws`에서 작업**합니다.

`gong_ws`에서 이미 프로젝트 기능을 많이 만들었다면 그 작업을 버리는 것이
아니라, 필요한 코드만 프로젝트용 패키지로 옮깁니다.

------------------------------------------------------------------------

## 4. 기존 실습 파일에서 이미 작업했다면?

예를 들어 기존에 다음 파일을 수정하면서 로봇팔 기능을 만들었다고
가정합니다.

``` text
~/gong_ws/src/tf2_basic/tf2_basic/tictactoe_pick.py
```

계속 이 위치에서 프로젝트를 개발하기보다는 프로젝트 저장소에 해당 기능을
담당할 패키지를 두고 코드를 이전합니다.

예:

``` text
~/team2/ros_ws/src/tictactoe_manipulator/
├── package.xml
├── setup.py
├── setup.cfg
├── resource/
│   └── tictactoe_manipulator
└── tictactoe_manipulator/
    ├── __init__.py
    └── tictactoe_pick.py
```

즉,

``` text
기존 실습 workspace
        │
        │ 필요한 프로젝트 코드 이전
        ▼
team2/ros_ws/src/프로젝트_패키지
```

입니다.

**실습 워크스페이스 전체를 복사하거나 merge하는 것이 아닙니다.**

------------------------------------------------------------------------

## 5. 브랜치는 계속 각자 사용합니다

폴더 구조를 통일한다고 해서 모두 한 브랜치에서 동시에 작업하는 것은
아닙니다.

각자 개인 브랜치는 계속 사용합니다.

예:

``` text
main
  │
  └── feature/integration       ← 통합/테스트 기준
       ↑
       ├── feature/board-vision
       ├── member-a
       ├── member-b
       └── member-c
```

차이는 **브랜치마다 서로 다른 워크스페이스를 가지는 것이 아니라, 같은
프로젝트 구조에서 서로 다른 기능을 수정한다는 것**입니다.

예:

``` text
feature/board-vision
└── ros_ws/src/tictactoe_vision/

member-a
└── ros_ws/src/tictactoe_manipulator/

member-b
└── ros_ws/src/tictactoe_game/
```

기능이 어느 정도 동작하면 `feature/integration`에 합쳐서 전체 연결을
확인합니다.

------------------------------------------------------------------------

## 6. 팀원이 실제로 해야 하는 작업

### Step 1. 기존 작업 백업

현재 브랜치의 변경사항을 먼저 commit/push합니다.

``` bash
git status
git add .
git commit -m "backup current work"
git push
```

아직 Git에 없는 파일도 반드시 확인합니다.

------------------------------------------------------------------------

### Step 2. 팀 프로젝트 저장소 준비

프로젝트 저장소가 아직 없다면 clone합니다.

``` bash
cd ~
git clone https://github.com/sbeetle1003-stack/team2.git
cd team2
```

이미 clone되어 있다면 새로 clone할 필요는 없습니다.

------------------------------------------------------------------------

### Step 3. 자신의 작업 브랜치로 이동

``` bash
git fetch --all
git checkout <자신의-브랜치>
```

예:

``` bash
git checkout feature/board-vision
```

------------------------------------------------------------------------

### Step 4. 프로젝트 기준 구조를 가져오기

팀에서 정한 통합 브랜치의 최신 구조를 확인합니다.

중요한 것은 **기존 작업을 바로 강제로 merge하기 전에 백업하고, 충돌
가능성을 확인하는 것**입니다.

팀에서 구조 정리가 완료된 뒤 각 브랜치가 `ros_ws/src` 기준 구조를
사용하도록 맞춥니다.

------------------------------------------------------------------------

### Step 5. 기존 실습 코드 중 필요한 부분만 이전

예를 들어:

``` text
기존
~/gong_ws/src/tf2_basic/...

        ↓ 필요한 프로젝트 코드만 이동

새 위치
~/team2/ros_ws/src/tictactoe_manipulator/...
```

실습에 사용했던 모든 파일을 무조건 가져오지 않습니다.

프로젝트 실행에 실제로 필요한 코드, launch, config, resource 등을
구분해서 가져옵니다.

------------------------------------------------------------------------

### Step 6. 프로젝트 workspace에서 빌드

``` bash
cd ~/team2/ros_ws
colcon build --symlink-install
source install/setup.bash
```

빌드 후 자신의 노드가 정상 실행되는지 확인합니다.

------------------------------------------------------------------------

### Step 7. 변경사항 commit/push

``` bash
git status
git add .
git commit -m "refactor: move project code into team workspace"
git push
```

------------------------------------------------------------------------

## 7. 절대경로를 사용하지 않습니다

다음처럼 자신의 컴퓨터에만 존재하는 경로를 코드에 직접 넣으면 안 됩니다.

``` python
# ❌ 사용하지 않기
model_path = "/home/jamiek/gong_ws/src/project2/models/model.sdf"
```

팀원마다 사용자 이름과 저장소 위치가 다르기 때문에 다른 컴퓨터에서
실행되지 않습니다.

ROS2 패키지 내부의 파일이라면 가능한 한 패키지 위치를 기준으로 찾습니다.

``` python
from ament_index_python.packages import get_package_share_directory
import os

pkg_share = get_package_share_directory("패키지명")
model_path = os.path.join(pkg_share, "models", "model.sdf")
```

이를 사용하려면 해당 resource가 설치되도록 `setup.py` 또는 CMake 설정도
함께 구성해야 합니다.

핵심은:

> **`/home/누구/...`가 아니라 ROS2 package를 기준으로 파일을 찾습니다.**

------------------------------------------------------------------------

## 8. `build`, `install`, `log`는 Git에 올리지 않습니다

`colcon build`를 실행하면 일반적으로 다음 폴더가 만들어집니다.

``` text
ros_ws/
├── build/
├── install/
├── log/
└── src/
```

이 중 Git으로 관리해야 할 핵심은 소스 코드인 `src/`입니다.

`.gitignore`에는 최소한 다음을 포함합니다.

``` gitignore
build/
install/
log/
```

`build`, `install`, `log`는 각자의 컴퓨터에서 다시 생성합니다.

------------------------------------------------------------------------

## 9. OpenManipulator 같은 기존 패키지는 어떻게 하나요?

프로젝트에서 사용한다고 해서 기존 ROS2/OpenManipulator 패키지를 전부
`team2` 저장소에 복사할 필요는 없습니다.

이미 별도 워크스페이스에 OpenManipulator가 정상적으로 설치되어 있다면
먼저 source한 뒤 팀 프로젝트 workspace를 사용할 수 있습니다.

예:

``` bash
source /opt/ros/jazzy/setup.bash
source ~/open_manipulator_ws/install/setup.bash

cd ~/team2/ros_ws
colcon build --symlink-install
source install/setup.bash
```

개념적으로는:

``` text
ROS2 Jazzy
    ↓
OpenManipulator workspace
    ↓
Team2 workspace
```

순서로 환경을 겹쳐 사용하는 것입니다.

단, **모든 팀원이 프로젝트 실행에 필요한 외부 패키지와 설치 방법을 알 수
있도록 README에 의존성을 기록해야 합니다.**

------------------------------------------------------------------------

## 10. 앞으로의 권장 작업 흐름

앞으로는 다음 과정을 반복합니다.

``` text
통합 브랜치의 최신 구조 확인
          ↓
자기 브랜치에서 담당 기능 개발
          ↓
작은 기능 단위로 commit
          ↓
push
          ↓
integration에 통합
          ↓
colcon build
          ↓
노드 연결/실행 테스트
          ↓
다시 개발
```

마지막 날에 모든 브랜치를 한 번에 합치는 방식은 피합니다.

------------------------------------------------------------------------

## 11. 팀에서 지금 통일해야 할 것

폴더만 맞추는 것으로 끝나지 않습니다. 최소한 다음 사항은 팀원끼리
합의해야 합니다.

1.  공식 프로젝트 workspace: `team2/ros_ws`
2.  모든 팀 프로젝트 ROS2 package 위치: `team2/ros_ws/src`
3.  각 팀원이 담당하는 package/기능
4.  topic 이름과 message type
5.  service/action 이름과 type
6.  launch 파일의 기준
7.  외부 dependency 설치 방법
8.  절대경로 사용 금지
9.  `build/`, `install/`, `log/` Git 제외
10. 기능 단위로 integration에 자주 통합하여 테스트

------------------------------------------------------------------------

## 12. 가장 중요한 개념 정리

### Q. `gong_ws`를 지워야 하나요?

**아니요.** 기존 실습용으로 남겨두면 됩니다.

### Q. 팀원마다 새 workspace를 하나씩 만들어야 하나요?

**서로 다른 workspace를 설계하는 것이 아닙니다.**\
모두 GitHub의 `team2/ros_ws`라는 동일한 구조를 각자의 컴퓨터에서
사용합니다.

### Q. 기존에 만든 코드는 버리나요?

**아니요.** 프로젝트에 필요한 코드를 새 프로젝트 구조의 적절한 패키지로
이전합니다.

### Q. 개인 브랜치는 없어지나요?

**아니요.** 개인 브랜치에서 계속 작업합니다. 단지 폴더 구조를 공통으로
맞춥니다.

### Q. 모든 코드가 완성된 뒤 옮기면 안 되나요?

가능은 하지만 권장하지 않습니다. 기능이 많아질수록 기존 경로와 실습
패키지에 대한 의존성이 늘어나서 마지막 통합이 훨씬 어려워질 수 있습니다.

### Q. 지금 당장 모든 기능을 하나로 합쳐야 하나요?

**아니요.**

지금 필요한 것은:

> **기능 전체 통합이 아니라 프로젝트 구조의 통일입니다.**

------------------------------------------------------------------------

# 한 문장으로 요약

> **기존 `gong_ws`는 실습/참고용으로 그대로 두고, 앞으로 팀 프로젝트는
> 모두 GitHub `team2` 저장소의 `ros_ws/src`라는 동일한 구조에서 각자
> 브랜치로 작업하며, 기존에 만든 프로젝트 코드만 필요한 패키지로 옮겨서
> 작은 단위로 계속 통합합니다.**
